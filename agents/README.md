# 🤖 AI Agents

Этот каталог содержит всех AI-агентов системы.

## Список Агентов

### 📈 Trend Scanner
**Путь**: `trend-scanner/`
**Задача**: Обнаружение новых трендов и бизнес-возможностей

**Источники данных**:
- Google Trends API
- Reddit (r/startups, r/SaaS, r/entrepreneur)
- Product Hunt
- Hacker News

**Выход**: JSON файлы с трендами и оценкой потенциала

---

### 💡 Business Generator
**Путь**: `business-generator/`
**Задача**: Превращение трендов в бизнес-идеи

**Функции**:
- Анализ конкурентов
- Оценка сложности реализации
- Расчёт потенциальной прибыльности
- Приоритизация идей

**Выход**: Структурированные бизнес-идеи с метриками

---

### 👨‍💻 Developer Agent
**Путь**: `developer/`
**Задача**: Разработка digital продуктов

**Функции**:
- Генерация технического задания
- Создание архитектуры
- Написание кода
- Деплой MVP

**Используемые инструменты**:
- Claude Code
- Cursor AI
- v0.dev

---

### 📣 Marketer Agent
**Путь**: `marketer/`
**Задача**: Создание и запуск маркетинговых стратегий

**Функции**:
- Генерация контента (landing pages, blog posts)
- SEO оптимизация
- Настройка рекламных кампаний
- Social media automation

---

### 💰 Sales Agent
**Путь**: `sales/`
**Задача**: Автоматизация продаж

**Функции**:
- Создание воронок продаж
- Email automation
- A/B тестирование
- Обработка заказов

---

## Общая Структура Агента

Каждый агент следует единой структуре:

```
agent-name/
├── __init__.py              # Инициализация модуля
├── agent.py                 # Главный класс агента
├── config.yaml              # Конфигурация агента
├── prompts/                 # Промпты для LLM
│   ├── analyze.txt
│   └── generate.txt
├── utils/                   # Утилиты специфичные для агента
├── tests/                   # Юнит-тесты
│   └── test_agent.py
└── README.md                # Документация агента
```

## Использование

### Запуск агента вручную

```python
from agents.trend_scanner.agent import TrendScannerAgent

agent = TrendScannerAgent()
trends = agent.scan()
print(f"Found {len(trends)} trends")
```

### Запуск через оркестратор

```python
from backend.orchestrator import Orchestrator

orchestrator = Orchestrator()
orchestrator.run_agent("trend-scanner")
```

## Разработка Нового Агента

1. Создать директорию в `/agents/<agent-name>/`
2. Скопировать шаблон из `/agents/shared/template/`
3. Реализовать метод `run()` в `agent.py`
4. Добавить промпты в `/prompts/`
5. Написать тесты
6. Обновить этот README

## Конвенции

### Промпты
- Хранить в отдельных `.txt` файлах в `/prompts/`
- Использовать переменные в формате `{variable_name}`
- Документировать ожидаемый output

### Логирование
- Использовать `agents.shared.logger`
- Log level: INFO для нормальной работы, DEBUG для разработки
- Логировать все LLM запросы для отладки

### Тестирование
- Юнит-тесты для каждого агента
- Mock LLM responses для быстрых тестов
- Интеграционные тесты с реальными API (опционально)

## Мониторинг

Все агенты автоматически логируют:
- Время выполнения
- Использование LLM (токены, стоимость)
- Успех/ошибки
- Output метрики

Dashboard доступен по адресу `/analytics/agents`

## Troubleshooting

### Агент не запускается
1. Проверить `.env` файл - все ключи API есть?
2. Проверить логи в `/logs/agents/<agent-name>.log`
3. Запустить с `DEBUG=true` для детальных логов

### Высокая стоимость LLM
1. Проверить кэширование (должно быть включено)
2. Использовать более дешёвые модели для рутинных задач
3. Оптимизировать промпты (меньше токенов)

### Низкое качество output
1. Улучшить промпты - добавить примеры
2. Использовать более мощные модели
3. Добавить validation output

---

**Следующие шаги**: См. [RESEARCH_PLAN.md](../docs/research/RESEARCH_PLAN.md)
