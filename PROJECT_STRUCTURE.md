# 📁 Структура Проекта AI Business Empire

## Обзор Директорий

```
project1/
├── docs/                      # 📚 Документация
├── agents/                    # 🤖 AI-агенты
├── frontend/                  # 🎨 Веб-интерфейс
├── backend/                   # ⚙️ Серверная логика
├── data/                      # 💾 Данные
├── scripts/                   # 🛠 Утилиты и скрипты
├── infrastructure/            # 🏗 Конфигурация инфраструктуры
└── config/                    # ⚙️ Конфигурационные файлы
```

---

## Детальное Описание

### 📚 `/docs` - Документация

Вся проектная документация.

```
docs/
├── architecture/              # Архитектурные решения
│   ├── SYSTEM_OVERVIEW.md    # Общий обзор системы
│   ├── AGENTS_DESIGN.md      # Дизайн агентов (TODO)
│   └── DATA_FLOW.md          # Потоки данных (TODO)
│
├── research/                  # Результаты исследований
│   ├── RESEARCH_PLAN.md      # План исследований
│   ├── tech_stack.md         # Выбор технологий (TODO)
│   └── case_studies/         # Изученные кейсы (TODO)
│
├── plans/                     # Планы и роадмапы
│   ├── ROADMAP.md            # Дорожная карта
│   ├── BUDGET_OPTIMIZATION.md # Оптимизация бюджета
│   └── sprint_plans/         # Планы спринтов (TODO)
│
└── api/                       # API документация (TODO)
    ├── openapi.yaml          # OpenAPI спецификация
    └── webhooks.md           # Вебхуки
```

**Когда использовать**:
- Архитектурные решения → `architecture/`
- Результаты исследований → `research/`
- Планирование → `plans/`

---

### 🤖 `/agents` - AI-Агенты

Код всех AI-агентов системы.

```
agents/
├── trend-scanner/             # 📈 Поиск трендов
│   ├── __init__.py
│   ├── agent.py              # Основная логика агента
│   ├── sources/              # Источники данных
│   │   ├── google_trends.py
│   │   ├── reddit_parser.py
│   │   ├── producthunt.py
│   │   └── hackernews.py
│   ├── analyzers/            # Анализ данных
│   │   ├── trend_scorer.py
│   │   └── llm_analyzer.py
│   ├── prompts/              # Промпты для LLM
│   │   └── analyze_trend.txt
│   └── tests/
│
├── business-generator/        # 💡 Генерация бизнес-идей
│   ├── __init__.py
│   ├── agent.py
│   ├── idea_generator.py     # Генерация идей
│   ├── competitor_analyzer.py # Анализ конкурентов
│   ├── scorer.py             # Оценка потенциала
│   ├── prompts/
│   └── tests/
│
├── developer/                 # 👨‍💻 Разработка продуктов
│   ├── __init__.py
│   ├── agent.py
│   ├── code_generator.py     # Генерация кода
│   ├── architect.py          # Архитектура приложения
│   ├── deployer.py           # Деплой
│   ├── templates/            # Шаблоны приложений
│   │   ├── saas_template/
│   │   └── landing_template/
│   ├── prompts/
│   └── tests/
│
├── marketer/                  # 📣 Маркетинг
│   ├── __init__.py
│   ├── agent.py
│   ├── content_generator.py  # Генерация контента
│   ├── seo_optimizer.py      # SEO оптимизация
│   ├── social_media.py       # Соцсети
│   ├── prompts/
│   └── tests/
│
├── sales/                     # 💰 Продажи
│   ├── __init__.py
│   ├── agent.py
│   ├── funnel_builder.py     # Воронки продаж
│   ├── email_automation.py   # Email маркетинг
│   ├── prompts/
│   └── tests/
│
└── shared/                    # 🔧 Общие утилиты
    ├── llm_client.py         # LLM клиент (Claude, GPT)
    ├── cache.py              # Кэширование
    ├── logger.py             # Логирование
    └── utils.py
```

**Структура каждого агента**:
- `agent.py` - главный класс агента
- `prompts/` - все промпты в отдельных файлах
- `tests/` - юнит-тесты
- Специфичные модули для каждой задачи

---

### 🎨 `/frontend` - Веб-Интерфейс

Next.js приложение для дашборда.

```
frontend/
├── src/
│   ├── app/                   # Next.js 14 App Router
│   │   ├── page.tsx          # Главная страница (dashboard)
│   │   ├── trends/           # Страница трендов
│   │   ├── businesses/       # Страница бизнесов
│   │   └── analytics/        # Аналитика
│   │
│   ├── components/            # React компоненты
│   │   ├── ui/               # UI компоненты (shadcn/ui)
│   │   ├── TrendCard.tsx     # Карточка тренда
│   │   ├── BusinessCard.tsx  # Карточка бизнеса
│   │   └── Dashboard.tsx     # Дашборд
│   │
│   ├── lib/                   # Утилиты
│   │   ├── api.ts            # API клиент
│   │   └── utils.ts
│   │
│   └── hooks/                 # React hooks
│       └── useBusinesses.ts
│
├── public/                    # Статические файлы
├── package.json
├── tsconfig.json
└── next.config.js
```

**Tech Stack**:
- Next.js 14 (App Router)
- TypeScript
- Tailwind CSS
- shadcn/ui компоненты
- React Query для data fetching

---

### ⚙️ `/backend` - Серверная Часть

FastAPI приложение и оркестратор.

```
backend/
├── api/                       # REST API
│   ├── __init__.py
│   ├── main.py               # FastAPI app
│   ├── routes/               # API endpoints
│   │   ├── trends.py
│   │   ├── businesses.py
│   │   ├── agents.py
│   │   └── analytics.py
│   ├── schemas/              # Pydantic модели
│   │   ├── trend.py
│   │   └── business.py
│   ├── middleware/
│   └── tests/
│
├── orchestrator/              # Координация агентов
│   ├── __init__.py
│   ├── orchestrator.py       # Главный оркестратор
│   ├── task_manager.py       # Управление задачами
│   ├── scheduler.py          # Планировщик (Celery)
│   └── workflows/            # Workflow определения
│       ├── trend_to_business.py
│       └── business_launch.py
│
├── database/                  # База данных
│   ├── __init__.py
│   ├── models.py             # SQLAlchemy модели
│   ├── migrations/           # Alembic миграции
│   └── seed.py               # Seed данные
│
├── requirements.txt
└── pyproject.toml
```

**Tech Stack**:
- FastAPI (async)
- SQLAlchemy (ORM)
- Alembic (миграции)
- Celery + Redis (очереди задач)
- Pydantic (validation)

---

### 💾 `/data` - Данные

Все данные системы (не в Git, кроме примеров).

```
data/
├── trends/                    # Обнаруженные тренды
│   ├── raw/                  # Сырые данные (игнор в git)
│   ├── processed/            # Обработанные
│   └── archive/              # Архив старых трендов
│
├── businesses/                # Данные бизнесов
│   ├── ideas/                # Сгенерированные идеи
│   ├── active/               # Активные бизнесы
│   ├── paused/               # На паузе
│   └── archived/             # Закрытые
│
├── analytics/                 # Аналитика и метрики
│   ├── daily_reports/
│   ├── monthly_reports/
│   └── exports/
│
└── examples/                  # Примерные данные (в git)
    ├── sample_trends.json
    └── sample_business.json
```

**Важно**:
- `/data` в `.gitignore` (кроме `/examples`)
- Backup ежедневно
- Retention policy: 90 дней

---

### 🛠 `/scripts` - Скрипты

Утилиты для автоматизации.

```
scripts/
├── setup/                     # Начальная настройка
│   ├── init_db.py            # Инициализация БД
│   ├── create_admin.py       # Создание админа
│   └── install_deps.sh       # Установка зависимостей
│
├── maintenance/               # Обслуживание
│   ├── backup_db.py          # Бэкап БД
│   ├── cleanup_old_data.py   # Очистка старых данных
│   └── health_check.py       # Проверка здоровья системы
│
├── development/               # Для разработки
│   ├── seed_db.py            # Наполнение тестовыми данными
│   └── reset_env.sh          # Сброс окружения
│
└── deployment/                # Деплой
    ├── deploy.sh             # Деплой скрипт
    └── rollback.sh           # Откат
```

---

### 🏗 `/infrastructure` - Инфраструктура

Конфигурация для деплоя и инфраструктуры.

```
infrastructure/
├── docker/                    # Docker конфигурация
│   ├── Dockerfile.backend
│   ├── Dockerfile.frontend
│   └── docker-compose.yml
│
├── kubernetes/                # K8s манифесты (если нужно)
│   ├── deployment.yaml
│   └── service.yaml
│
├── terraform/                 # Infrastructure as Code
│   ├── main.tf
│   └── variables.tf
│
└── monitoring/                # Мониторинг
    ├── prometheus.yml
    └── grafana/
```

---

### ⚙️ `/config` - Конфигурация

Конфигурационные файлы.

```
config/
├── default.yaml               # Дефолтные настройки
├── development.yaml           # Dev окружение
├── production.yaml            # Prod окружение
├── agents_config.yaml         # Настройки агентов
└── prompts_config.yaml        # Настройки промптов
```

**Примечание**: Секреты в `.env`, не в YAML!

---

## 🔄 Workflow: Где Писать Код

### Новый агент?
→ `/agents/<agent-name>/`

### Новая страница в UI?
→ `/frontend/src/app/<page-name>/`

### Новый API endpoint?
→ `/backend/api/routes/<resource>.py`

### Новый workflow?
→ `/backend/orchestrator/workflows/`

### Документация?
→ `/docs/<category>/`

### Утилитный скрипт?
→ `/scripts/<category>/`

---

## 📝 Соглашения

### Именование файлов:
- Python: `snake_case.py`
- TypeScript/React: `PascalCase.tsx` (компоненты), `camelCase.ts` (утилиты)
- Конфиги: `lowercase.yaml`
- Документация: `UPPERCASE.md` (главные), `lowercase.md` (вспомогательные)

### Структура агента:
Каждый агент - отдельная директория с:
- `agent.py` (главный класс)
- `prompts/` (промпты отдельно)
- `tests/` (тесты обязательно)

### Git branches:
- `main` - продакшен
- `develop` - разработка
- `feature/<name>` - новые фичи
- `fix/<name>` - багфиксы

---

## 🚀 Быстрый Старт

```bash
# 1. Клонировать репо
git clone <repo-url>
cd project1

# 2. Backend setup
cd backend
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt
python scripts/setup/init_db.py

# 3. Frontend setup
cd ../frontend
npm install
npm run dev

# 4. Запустить агентов
cd ../agents
python trend-scanner/agent.py
```

---

**Последнее обновление**: 2026-02-06
